/*
Meteor.startup(function () {
    Products._ensureIndex({
        "name": "text"
    });

    Customers._ensureIndex({
        "name": "text"
    });

    Meteor.users._ensureIndex({
        "username": "text"
    });
});*/
